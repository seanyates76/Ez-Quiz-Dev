name: Dependency Review

on:
  pull_request:
    branches: [ "main", "beta" ]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Dependency review temporarily disabled
        run: |
          echo "Dependency review action disabled because repository security features are unavailable."
